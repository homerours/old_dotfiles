# Fonctions

# Prepend sudo to a command
function run-with-sudo() {
    LBUFFER="sudo $LBUFFER"
}

# Back
function last-directory() {
    builtin cd $OLDPWD
        zle reset-prompt
}

# Create directory and move into
function mkcd() {
    mkdir $1 && cd $1
}
